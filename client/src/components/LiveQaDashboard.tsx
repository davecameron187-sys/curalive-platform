// Stub — restore LiveQaDashboard from pre-force-push commit
export default function LiveQaDashboard() {
  return (
    <div style={{ padding: "24px", color: "#94a3b8" }}>
      Live Q&A Dashboard — restoring from backup
    </div>
  );
}
