# CuraLive — Living Session Brief
**Last updated:** 19 April 2026 — End of Day Final
**Updated by:** Claude (Chief Architect)
**Branch:** `RenderMigration`
**Repo:** `davecameron187-sys/curalive-platform`
**Live URL:** `https://curalive-node.onrender.com`

---

## HOW TO START A NEW SESSION

1. Open ChatGPT first
2. Say: *"Please fetch the file `CURALIVE_BRIEF.md` from GitHub repo `davecameron187-sys/curalive-platform` branch `RenderMigration` and paste the full contents."*
3. Copy what ChatGPT returns
4. Open Claude and paste it with: *"Here is the CuraLive brief. Pick up from here."*
5. Claude is instantly up to speed — no repeated explanations, no lost context

---

## TOOL STACK & RULES — NON-NEGOTIABLE

| Tool | Role |
|---|---|
| **Claude** | Chief architect, all decisions, code writing, brief updates |
| **Replit** | Critical file edits, git push via Shell, complex terminal tasks |
| **ChatGPT** | Small new files, GitHub file reads, web searches |
| **GitHub RenderMigration** | Single source of truth — always |
| **Render** | Auto-deploys on every push to RenderMigration |

**Git push command from Replit Shell:**
```
git push github RenderMigration
```

**Rules — non-negotiable:**
- Before building or fixing anything, ChatGPT reads the current file from GitHub first
- Never replace a working file from scratch — always preserve existing functionality
- One task at a time to ChatGPT — small commands only
- Replit handles all critical file edits and git operations
- Brief updated at end of every session — no exceptions
- NEVER force push without Claude verifying GitHub has no commits ahead of Replit

---

## CRITICAL LESSONS LEARNED — 19 APRIL 2026

1. **Force push wiped working commits** — always verify GitHub has no commits ahead of Replit before force pushing
2. **Never rewrite working files from scratch** — read current file first, preserve functionality
3. **ChatGPT hallucinates file contents** — always verify against actual GitHub files
4. **Screenshots are documentation** — screenshot working features and keep them as reference
5. **Render pipeline minutes** — Professional plan upgraded but build stuck in failed state. Awaiting Render support to clear failed state.

---

## CURRENT DEPLOY STATE

| Check | Status |
|---|---|
| Server running | ✓ Live on current deploy |
| `/ping` returns pong | ✓ |
| tRPC API responding | ✓ |
| Frontend loads | ✓ |
| Shadow Mode V3 | ✓ Live |
| Session creation | ✓ Working |
| Recall.ai bot joins | ✓ eu-central-1 region confirmed |
| Ably real-time | ✓ Connected |
| Webhook fix | ⚠ Fix on GitHub but NOT deployed — Render stuck |
| Bot status "bot joining" | ⚠ Stuck — webhooks being rejected until fix deploys |

---

## RENDER — URGENT ACTION NEEDED TOMORROW

Render is stuck in failed deploy state. Pipeline minutes were exhausted before Professional plan upgrade. Support email sent 19 April 2026.

**First thing tomorrow:**
1. Check Render support response
2. Once cleared — trigger manual deploy
3. Confirm webhook fix deploys — look for `[Recall] accepting webhook without signature verification` in logs
4. Test session — confirm bot status changes from "bot joining" to "live"

---

## ENVIRONMENT VARIABLES — RENDER

| Variable | Status |
|---|---|
| `RECALL_AI_API_KEY` | ✓ Updated — CuraLive Production key |
| `RECALL_AI_BASE_URL` | ✓ `https://eu-central-1.recall.ai/api/v1` |
| `ABLY_API_KEY` | ✓ Set and connected |
| `RECALL_AI_WEBHOOK_SECRET` | ✓ Set — but webhook fix not yet deployed |
| `OAUTH_SERVER_URL` | ⚠ Not set — auth bypass active |

---

## SHADOW MODE V3 — CURRENT STATE

### Working
- Live Console — session list, NEW SESSION form, intelligence feed, PSIL indicator, END SESSION button
- Live Q&A — wired to LiveQaDashboard
- History — completed sessions
- Floating CuraLive Assistant
- Session creation — Client Name, Event Name, Event Type, Platform, Meeting URL, Notes
- Platforms — Zoom/Teams/Webex/Google Meet (Recall.ai), Chorus Call/Other (local capture)

### Placeholder
- Participants tab — designed, brief ready to build
- Pre-Event tab — coming next

---

## PARTICIPANTS TAB — DESIGNED, READY TO BUILD

Design confirmed. Shows:
- Stats bar — total, executives, analysts, operator count
- Currently speaking section — highlighted green border
- On call section — name, company, role, speaking time bar, status badge
- Role types — Executive (green), Analyst (blue), Operator (purple)
- Data source — Recall.ai participant join/leave and speaking events via webhooks

Brief will be written by Claude and sent to Replit once Render is unblocked.

---

## KNOWN ISSUES — OPEN

| # | Issue | Priority |
|---|---|---|
| 1 | Render stuck in failed state — awaiting support | URGENT |
| 2 | Webhook fix on GitHub but not deployed | URGENT |
| 3 | Bot stays on "bot joining" until webhook fix deploys | BLOCKED |
| 4 | Participants tab — not yet built | NEXT |
| 5 | Pre-Event tab — not yet built | NEXT |
| 6 | Broadcast Control — needs rebuild (lost in force push) | NEXT |
| 7 | CuraLive Assistant — UI only, AI not wired | LATER |
| 8 | `OAUTH_SERVER_URL` not set — auth bypass active | DEFERRED |

---

## NEXT SESSION PRIORITIES

1. **Check Render support response — clear failed state**
2. **Trigger manual deploy — confirm webhook fix live**
3. **Test full pipeline — session → bot joins → status live → intelligence feed receives data**
4. **Build Participants tab — brief ready**
5. **Rebuild Broadcast Control**
6. **Build Pre-Event tab**
7. **Meridian Resources demo data**

---

## FILES — CURRENT STATE

| File | Status |
|---|---|
| `client/src/pages/ShadowMode.tsx` | ✓ V3 ~400 lines |
| `client/src/components/LiveQaDashboard.tsx` | ✓ Rebuilt |
| `client/src/pages/AttendeeQA.tsx` | ✓ Rebuilt |
| `server/routers/liveQaRouter.ts` | ✓ Rebuilt |
| `server/services/PlatformEmbedService.ts` | ✓ Rebuilt |
| `server/routers/shadowModeRouter.ts` | ✓ Working |
| `server/recallWebhook.ts` | ✓ Fixed on GitHub — awaiting deploy |
| `server/webhooks/recall.ts` | ✓ Fixed on GitHub — awaiting deploy |
| `server/routers/broadcastControlRouter.ts` | ✗ Needs rebuild |
| `client/src/components/BroadcastControl.tsx` | ✗ Needs rebuild |
| `CURALIVE_BRIEF.md` | ✓ Living brief on GitHub |

---

## PATENT NOTE

- South African Provisional Application filed March 2026
- PCT deadline: March 2027 — via CIPC direct (not PatentPC)
- 19 invention families, 125 claims, 18 figures
- Four new disclosures to add:
  1. Participant Intelligence Layer
  2. Disclosure Consistency Engine
  3. Pre-Speak Intelligence Lock (PSIL)
  4. Communication Genome

---

*This file is the single source of truth for session context. Update it at the end of every session without fail.*
