Here’s the exact brief you can upload to Replit.

---

# CuraLive — Replit Validation Brief

## Embedded Shadow Mode + WebPhone + Live Console Validation

### Purpose

Validate the **new embedded Shadow Mode live operator console** and the recent staging fixes **without rebuilding the product**.

This is a **validation and selective refinement pass only**.

Manus has already completed:

* the targeted staging fixes,
* WebPhone/webcast embedding into Shadow Mode,
* and the new **full embedded live operator console** experience.  

---

# Critical instruction

## Replit does **not** need to redo everything.

Please do **not**:

* rebuild Shadow Mode
* redesign the live console
* rewrite WebPhone integration
* re-implement archive/export/handoff
* do broad refactors

## Replit should only:

1. pull the latest `ManusChatgpt` branch
2. run the app
3. validate the new embedded Shadow Mode + live console behavior
4. report real bugs
5. make only **minimal targeted fixes** if a real runtime issue requires it

This is a cost-control requirement.

---

# Current implementation status

## Already completed by Manus

### Staging fixes

* BUG-A1 fixed
* BUG-B1 fixed
* BUG-D2 resolved as false positive
* Chorus Call dropdown intentionally deferred for now 

### Embedded WebPhone/Webcast in Shadow Mode

* WebPhone controls embedded into Shadow Mode
* provider/fallback state visible
* collapsible WebPhone controls section added
* Shadow Mode positioned as unified operator control room for webcast/audio sessions 

### Shadow Mode live console update

* new **LiveSessionPanel.tsx**
* full-screen live operator console modal
* tabs for **WebPhone / Q&A / Transcript / Notes**
* quick actions sidebar
* provider state bar
* session stats
* export / handoff / close controls
* “Open Live Console” button in Shadow Mode
* live session alert banner in Shadow Mode 

---

# Your validation scope

You are validating **this new live operator experience**:

### Shadow Mode now should support:

* archive/session management
* one-click **Open Live Console**
* embedded WebPhone controls
* provider status visibility
* live Q&A moderation
* live transcript viewing
* notes during session
* quick operator actions
* archive functionality still intact below the live console 

---

# Setup steps

Run:

```bash id="hrmu4v"
git fetch origin
git checkout ManusChatgpt
git pull origin ManusChatgpt
pnpm install
pnpm db:push
pnpm dev
```

If the branch only exists remotely:

```bash id="7gxmj0"
git checkout -b ManusChatgpt origin/ManusChatgpt
```

---

# Main validation areas

## Track 1 — Shadow Mode entry and live console launch

### Validate

* Shadow Mode loads correctly
* live session alert banner is visible at the top
* “Open Live Console” button is visible
* clicking “Open Live Console” opens the full-screen modal
* modal closes cleanly and returns to Shadow Mode

### Pass means

* operator can enter the live console from Shadow Mode in one click 

---

## Track 2 — Live console layout and operator workflow

### Validate

Inside the live console, confirm:

* session header shows:

  * event name
  * live/scheduled/ended status
  * duration
  * attendee count
* provider state bar displays correctly
* right sidebar shows:

  * quick actions
  * session stats
  * provider info
* footer shows:

  * Export
  * Handoff
  * Close Console

### Pass means

* the embedded console feels like a complete operator workspace, not a partial mockup 

---

## Track 3 — WebPhone tab validation

### Validate

* WebPhone tab is present
* embedded `WebPhoneCallManager` loads
* participant list appears
* connection quality displays
* call duration displays
* mute/unmute controls work or render correctly
* remove participant controls render correctly
* collapsible WebPhone controls section works from Shadow Mode session detail as applicable

### Pass means

* WebPhone controls are truly embedded in the operator workflow

---

## Track 4 — Provider state and fallback visibility

### Validate

* provider badge shows correct provider

  * WebPhone / Teams / Zoom / Webex / RTMP / PSTN
* provider status shows correctly

  * active / degraded / fallback / failed
* fallback notification appears if applicable
* fallback reason displays clearly
* provider info in sidebar matches header state
* no hidden provider switching

### Pass means

* operator can always see current connectivity state and fallback condition clearly

---

## Track 5 — Q&A tab validation

### Validate

* Q&A tab is present
* pending questions render
* approved questions render
* approve/reject controls appear
* pending/approved counts update properly
* no lag or broken UI from recent bulk-action fixes
* basic moderation flow works

### Pass means

* live Q&A moderation is usable inside the live console 

---

## Track 6 — Transcript tab validation

### Validate

* Transcript tab is present
* transcript entries render
* speaker name, text, timestamp display clearly
* empty state appears when no transcript exists
* transcript view is readable and stable
* no console errors from transcript rendering

### Pass means

* the operator can monitor the session transcript inside the live console 

---

## Track 7 — Notes tab validation

### Validate

* Notes tab is present
* textarea appears
* notes can be entered
* Save Notes button works or behaves correctly
* notes persist within the session lifecycle as currently implemented
* no UI breakage or console errors

### Pass means

* operator can take notes during the live session from inside the console 

---

## Track 8 — Quick actions sidebar

### Validate

* Mute All button appears
* Send Message button appears
* View Analytics button appears
* session stats render correctly
* provider info renders correctly
* layout remains usable and not cramped

### Pass means

* quick actions are available and clearly placed for operators 

---

## Track 9 — Regression check on archive/session management

### Validate

After opening/closing the live console:

* archived sessions are still visible
* session list still works
* archive/session detail still works
* no regressions in existing Shadow Mode archive flows
* export/handoff/archive functionality still behaves correctly

### Pass means

* the new live console did not break the existing Shadow Mode archive/operator workflow

---

# Known not-yet-implemented areas

These are **not** failures unless they break the current staging flow.

Do not treat these as regressions by default:

* Ably real-time live updates
* database persistence for provider/live session metadata
* full integration tests
* operator documentation/training
* auto-dismissing notifications after 10 seconds

Your job is to validate the current implemented state, not mark pending future phases as bugs.

---

# When to fix something directly

## Replit may fix directly only if:

* it is a small runtime/config/UI bug
* it clearly blocks this validation pass
* the fix is narrow and low-risk
* it does not require re-architecting the feature

## Replit should report back instead of fixing if:

* it requires broad workflow redesign
* it means rebuilding Shadow Mode/live console architecture
* it belongs to future phases already marked pending
* it is a product decision, not a runtime defect

---

# Bug reporting format

For every issue found, use this exact format:

**Title:**
Short bug summary

**Area:**
Live Console / WebPhone / Q&A / Transcript / Notes / Provider State / Archive / Shadow Mode / UX

**Severity:**
P0 / P1 / P2

**Expected:**
What should happen

**Actual:**
What happened

**Steps to Reproduce:**
Exact numbered steps

**Evidence:**
Screenshot / logs / video

**Likely File(s):**
If known

---

# Specific things to watch for

Please pay special attention to:

* whether the live console actually opens from Shadow Mode cleanly
* whether WebPhone controls are visible where expected
* whether provider/fallback state is easy to understand
* whether the live console feels embedded, not bolted on
* whether the archive/session experience remains stable after adding the live console
* whether there are console warnings/errors from the new UI

---

# Deliverables back from Replit

Please return:

1. pass/fail for each validation track above
2. screenshots or evidence for major flows
3. any bugs found
4. any minimal fixes applied
5. clear note on what still needs Manus rather than Replit

---

# Definition of success

This validation pass is successful when:

* the embedded live console works from inside Shadow Mode
* WebPhone controls are visible and usable
* provider/fallback state is clearly shown
* Q&A / Transcript / Notes tabs all function as expected
* archive/session management is not regressed
* only real defects are reported
* no unnecessary rebuild work is done

---

# Final instruction

This is a **validation pass for the new embedded Shadow Mode live console**.

Replit’s role is to:

* confirm it works,
* catch real bugs,
* and keep the work lean.

Do **not** redo completed work.
Validate the live console update, the WebPhone embedding, and the Shadow Mode integration exactly as implemented.

---

If you want, I can also turn this into a **shorter upload-ready markdown version** with no extra commentary.
