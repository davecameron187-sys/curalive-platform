CuraLive — Replit Manual Apply + Validation Brief
Integrated Live Console Build
No GitHub Access Required
Objective

Apply the completed manual delivery package from Manus into the Replit environment and run a validation-only pass.

This is not a rebuild task.
This is not a redesign task.
This is a manual paste + validate task only.

Manus has now delivered the full manual package in 3 parts, covering all required files for the integrated live console build. The delivery package is marked complete, with 12 files total and a defined apply order.

Critical instruction
Replit should not:
redesign Shadow Mode
rebuild the live console
invent missing architecture
rewrite the delivered features
apply broad refactors
Replit should only:
paste the delivered files into the correct paths
build/typecheck
validate the delivered integrated live console
report real runtime issues only
make only minimal targeted fixes if a blocking runtime issue absolutely requires it

The operating model remains:

Manus builds
Replit proves
Manual delivery package status

The manual delivery package is now complete.

Total files delivered
Part 1: 1 file
Part 2: 8 files
Part 3: 3 files
Total: 12 files delivered
Final apply order

Apply in this sequence:

Backend routers first
server/routers/session.ts
server/routers/analytics.ts
server/routers.ts
Hooks and services second
client/src/hooks/useAblySessions.ts
client/src/hooks/useKeyboardShortcuts.ts
client/src/services/sessionAutoSave.ts
Components third
client/src/components/ProviderStateIndicator.tsx
client/src/components/WebPhoneCallManager.tsx
client/src/components/LiveSessionPanel.tsx
Page integration last
client/src/pages/ShadowMode.tsx
Supporting files confirmation

Manus explicitly confirms that no additional supporting files are required beyond Parts 1, 2, and 3.

What this delivery is supposed to enable

Once the files are pasted correctly, the integrated live console should support:

live console entry/exit from Shadow Mode
Ably real-time updates
keyboard shortcuts
session auto-save and recovery
analytics display
export workflow
handoff workflow
provider state management
no regressions in existing Shadow Mode archive/session workflows
Apply checklist
Step 1 — Paste the delivered files

Paste the files from the 3-part Manus manual delivery package into the correct project paths in the apply order above.

Step 2 — Confirm files exist

Verify these files now exist in Replit:

server/routers/session.ts
server/routers/analytics.ts
server/routers.ts
client/src/hooks/useAblySessions.ts
client/src/hooks/useKeyboardShortcuts.ts
client/src/services/sessionAutoSave.ts
client/src/components/ProviderStateIndicator.tsx
client/src/components/WebPhoneCallManager.tsx
client/src/components/LiveSessionPanel.tsx
client/src/pages/ShadowMode.tsx
Step 3 — Build and typecheck

Run:

install dependencies if needed
typecheck
build

The package’s validation instructions explicitly call for:

pnpm install
pnpm tsc --noEmit
pnpm build
Validation scope

Tracks 1 and 9 were already passing before the missing-code issue.

So after manual paste, Replit should focus on validating Tracks 2–8, while still checking that 1 and 9 remain intact.

Track 2 — Ably Real-Time

Validate:

Ably client initializes
Q&A updates stream live
transcript updates stream live
provider updates reflect
participant events reflect
fallback to polling works if Ably disconnects
Track 3 — Keyboard Shortcuts

Validate:

M = mute all
A = approve next pending Q&A
R = reject next pending Q&A
S = save notes
E = export session
H = handoff session
? = show help dialog
shortcuts do not fire in text fields
Track 4 — Session Auto-Save & Recovery

Validate:

notes auto-save every 30 seconds
localStorage recovery exists
recovery prompt appears on reconnect
Restore works
Discard works
active tab restores correctly
Track 5 — Analytics Display

Validate:

engagement score visible in header
Q&A approval rate visible in header
metrics update every 5 seconds
no analytics-related console errors
Track 6 — Export Workflow

Validate:

Export button works
JSON file downloads
correct filename
session data included
Track 7 — Handoff Workflow

Validate:

Handoff button works
target operator selection works
handoff notes can be used
session ownership transfers
original console closes after success
Track 8 — Provider State

Validate:

provider status displays
connection quality displays
latency displays
fallback reason displays when applicable
provider state updates correctly
Track 9 — Full Regression

Reconfirm:

archive/session listing intact
search/filter intact
pagination intact
detail panel accessible
no regressions in existing Shadow Mode functionality
What not to do

Do not:

rebuild features already delivered
invent new code not required for validation
“improve” architecture during this pass
replace the operating model with independent Replit development

If something is broken because the delivered code itself is wrong, report it.
If something is missing from the delivered package, report it.
Do not compensate by rebuilding the product.

Bug reporting format

For each issue found, return:

Title:
Area:
Severity: P0 / P1 / P2
Expected:
Actual:
Steps to Reproduce:
Evidence:
Likely File(s):
Classification: runtime bug / manual delivery issue / needs Manus follow-up

Deliver back

Please return:

whether all 10 manual files were pasted successfully
build/typecheck result
pass/fail for Tracks 2–8
confirmation that Tracks 1 and 9 still hold
any real runtime bugs found
any minimal targeted fixes applied
anything that still needs Manus rather than Replit
Definition of success

This Replit pass is successful when:

the full manual delivery package is applied
the project builds
Tracks 2–8 are validated against the real integrated live console
Tracks 1 and 9 remain safe
no unnecessary rebuild work is done
Final instruction

The manual delivery package is now complete.
Please apply the delivered files and validate only.

Do not redesign or rebuild the feature set.
Use the delivered code, prove what works, and report only real blockers.