# CuraLive — New Agent Upload & Configuration Brief
Date: 2026-03-26

## STEP 1: Pull Latest Code from GitHub

The latest main branch has critical publish-safe fixes. Pull it now:

git pull origin main
pnpm install

What was just pushed (commit a2053f2):
- /health endpoint added (Replit deployment health check requires this)
- Server binds to 0.0.0.0 (not localhost — Replit proxy can't reach localhost)
- Legacy localStorage write removed from useAuth hook

## STEP 2: Verify App Starts

Start the app:
NODE_ENV=development pnpm exec tsx watch server/_core/index.ts

You should see in logs:
- "Server running on http://0.0.0.0:5000/"
- "[Shadow Mode] ✓ All critical environment variables configured"
- "[HealthGuardian] Starting autonomous monitoring"

Verify /health endpoint returns { status: "ok", timestamp: "..." }

## STEP 3: Verify Database Has Data

Run these SQL queries to confirm restored data:
- SELECT count(*) FROM health_checks; → should be ~4206+
- SELECT count(*) FROM archive_events; → should be 4
- SELECT count(*) FROM iso27001_controls; → should be 32
- SELECT count(*) FROM soc2_controls; → should be 36

## STEP 4: What's Working Without API Keys

Everything UI-related works:
- Operator Dashboard (/) with all tabs (Overview, Shadow Mode, Events, Partners, Billing, Settings)
- Intelligence Suite (/intelligence-suite) — 11 AI algorithms
- Archive & Reports — 4 events with transcripts and AI reports
- Transcript/recording download buttons
- Health monitoring system
- All 20 AI module definitions
- Billing, Events Hub, Partners sections

## STEP 5: API Keys to Configure (Priority Order)

HIGH PRIORITY:
1. ABLY_API_KEY — Real-time WebSocket transcript streaming. Without it, Shadow Mode falls back to 3-second polling. Get from ably.com (free tier works for dev).
2. RESEND_API_KEY — Email report delivery. Get from resend.com (100 emails/day free).

MEDIUM PRIORITY (when ready):
3. STRIPE_SECRET_KEY — Payment processing (billing UI works without it)
4. AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY — S3 recording storage (local storage works for now)
5. TWILIO_ACCOUNT_SID + TWILIO_AUTH_TOKEN + TWILIO_API_KEY + TWILIO_API_SECRET + TWILIO_TWIML_APP_SID + TWILIO_CALLER_ID — OCC telephony

LOW PRIORITY:
6. TELNYX_API_KEY + TELNYX_SIP_USERNAME + TELNYX_SIP_PASSWORD — Backup carrier for telephony
7. OAUTH_SERVER_URL + OWNER_OPEN_ID — Production auth (dev mode bypass is fine for now)
8. JWT_SECRET — Session cookie signing (set when enabling auth)

ALREADY CONFIGURED:
- DATABASE_URL — PostgreSQL (Replit Helium)
- RECALL_AI_API_KEY — Recall.ai bot deployment
- MUX_WEBHOOK_SECRET — Video webhooks
- OpenAI — Connected via Replit integration

## CRITICAL CODE RULES (MUST FOLLOW)

1. All tRPC imports use "../_core/trpc" — never import from @trpc directly
2. New routers MUST be registered in BOTH server/routers.eager.ts AND server/routers.ts
3. Toast notifications use sonner ONLY
4. rawSql() auto-appends RETURNING id and translates ? to $1/$2
5. shadow_sessions timestamps (started_at, ended_at) are bigint epoch milliseconds
6. attendee_registrations has camelCase columns — MUST double-quote in raw SQL
7. AI reports stored in archive_events with event_id format: shadow-{sessionId}
8. Never change primary key ID column types (serial ↔ varchar)
9. Use pnpm run db:push to sync schema — never write manual SQL migrations
10. Server MUST bind to 0.0.0.0 (not localhost) for Replit
11. /health endpoint MUST exist — Replit deployment checks it
12. Do NOT edit .replit file from code — use Replit UI only

## PUBLISH CHECKLIST (Before Publishing)

- [ ] App loads at /
- [ ] /health returns { status: "ok" }
- [ ] /operator-links loads
- [ ] Shadow Mode tab loads (/?tab=shadow-mode)
- [ ] Archive events visible in Archives & Reports
- [ ] No console errors on page load
- [ ] NODE_ENV will be "production" when published (auth bypass disabled)
- [ ] DATABASE_URL is set and working

## ARCHITECTURE SUMMARY

- Frontend: React 19 + Vite + TailwindCSS 4 + Shadcn/UI + Wouter routing
- Backend: Express + tRPC (86 routers), port 5000 dev / auto in prod
- Database: PostgreSQL via Drizzle ORM (100+ tables)
- Real-time: Ably WebSockets (with polling fallback)
- AI: OpenAI GPT-4o via Replit integration
- Package manager: pnpm 10.4.1
- Build: Vite (frontend) + esbuild (server)

## KEY FILES

- server/_core/index.ts — Express server entry, REST endpoints, /health
- server/_core/trpc.ts — tRPC init, auth bypass logic
- server/routers.ts + server/routers.eager.ts — Router registration (both must match)
- server/routers/shadowModeRouter.ts — Shadow Mode (989 lines)
- client/src/pages/ShadowMode.tsx — Shadow Mode UI (3676 lines)
- client/src/pages/Dashboard.tsx — Main dashboard with tabs
- drizzle/schema.ts — All database tables
- server/db.ts — DB connection with PG var fallback

## DO NOT

- Run git reset --hard or force-push
- Change pnpm version in workflow files to "version: 10"
- Alter packageManager field in package.json
- Create manual SQL migrations
- Change the /health endpoint or 0.0.0.0 binding
- Set AUTH_BYPASS=true in production secrets